apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    backstage.io/kubernetes-id: ${{ values.appName }}
  name: hello-tomcat
spec:
  template:
    spec:
      containers:
        - image: image-registry.openshift-image-registry.svc:5000/${{ values.namespace }}/hello-tomcat:stable
          name: hello-tomcat
          imagePullPolicy: Always
